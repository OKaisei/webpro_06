<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>トランスフォーマー 個体一覧</title>
</head>
<body>
    <h2>トランスフォーマー 一覧</h2>
    <table>
        <tr><th>ID</th><th>英語名</th><th>日本語名</th><th>陣営</th><th>詳細</th><th>編集</th></tr>
        <% let i = 0; %>
        <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.name_en %></td>
            <td><%= row.name_jp %></td>
            <td><%= row.faction %></td>
            <td><a href="/transformers/<%= i %>">詳細</a></td>
            <td><a href="/transformers/edit/<%= i %>">編集</a></td>
        </tr>
        <% i++; %>
        <% } %>
    </table>
    <br>
    <a href="/public/tf_add.html">新規個体の登録はこちら</a>
</body>
</html>